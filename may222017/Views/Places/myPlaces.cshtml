@model PagedList.IPagedList<may222017.Models.Places>
@using PagedList.Mvc;

@{
    ViewBag.Title = "myPlaces";
    Layout = "~/Views/Shared/_mytemplate.cshtml";
}


@*<div id="Place-Cards" class="container">

    <div class="row">
        <h2>All Venues</h2>

        @foreach (var item in Model)
        {

            <div class="col-md-4">

                <div class="ui card">
                    <div class="image">
                        @foreach (var img in item.Links)
                        {

                            //<img src="~/images/@img.Link.ToString()" />
                            string imageBase64 = Convert.ToBase64String(img.ImageData);
                            string imageSrc = string.Format("data:image/gif;base64,{0}", imageBase64);
                            <img src="@imageSrc" />
                            break;

                        }

                    </div>
                    <div class="content">
                        <a href='@(Url.Action("Venue","Places",new { id = item.Id}))' class="header">@item.PlaceName</a>
                        <div class="meta">
                            <span class="date">Price Range: @item.MinPriceRange - @item.MaxPriceRange</span>
                        </div>
                        <div class="description">
                            <b>Address: </b> @item.Address <br />
                            <b>Website: </b> @item.Website <br />
                            <b>Email: </b> @item.Email <br />
                        </div>
                    </div>
                    <div class="extra content">
                        @*<a>
                                <i class="user icon"></i>
                                22 Friends
                            </a>*@
                    @*</div>
                </div>
            </div>

        }
    </div>
</div>*@

<div id="Place-Cards" class="container">

    <div class="row">
        <div class="col-md-6">
            <h2>My Venues</h2>          
        </div>
    </div>

    <div class="row">

        @foreach (var item in Model)
        {

            <div class="col-md-6">

                <div style="margin-bottom: 8% !important;width:100%;" class="ui card">
                    <div style="max-height:300px; height:auto !important" class="right floated image">
                        @foreach (var img in item.Links)
                        {

                            //<img src="~/images/@img.Link.ToString()" />
                            string imageBase64 = Convert.ToBase64String(img.ImageData);
                            string imageSrc = string.Format("data:image/gif;base64,{0}", imageBase64);
                            <img style="max-height:300px; height:auto !important" src="@imageSrc" />
                            break;

                        }

                    </div>
                    <div class="left floated content">
                        <a href='@(Url.Action("Venue","Places",new { id = item.Id}))' class="header">@item.PlaceName</a>
                        <div class="meta">
                            <span class="date">Price Range: @item.MinPriceRange - @item.MaxPriceRange</span>
                        </div>
                        <div class="description">
                            <b>Address: </b> @item.Address <br />
                            <b>Website: </b> @item.Website <br />
                            <b>Email: </b> @item.Email <br />
                        </div>
                    </div>
                    <div class="extra content">
                        <div class="ui two buttons">
                            <div class="ui basic green button"><a href='@Url.Action("Edit","Places",new { id = item.Id})'>Edit</a></div>
                            <div class="ui basic red button"><a href='@Url.Action("Delete","Places",new { id = item.Id})'>Delete</a></div>
                        </div>
                    </div>
                </div>
            </div>

        }
    </div>
</div>








<div id="loader">
    <div class="spinner">
        <div class="dot1"></div>
        <div class="dot2"></div>
    </div>
</div>

<br />
<div class="text-center" id="pagination">
    Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

    @Html.PagedListPager(Model, page => Url.Action("Index",
    new { page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }))
    </div>

    <!-- Plugin JavaScript -->
    <script src="http://code.jquery.com/jquery-1.10.2.min.js" type="text/javascript"></script>
    <script src="@Url.Content("~/Content/mytemplate/")js/jquery.easing.1.3.js"></script>
    <script src="@Url.Content("~/Content/mytemplate/")js/classie.js"></script>
    <script src="@Url.Content("~/Content/mytemplate/")js/count-to.js"></script>
    <script src="@Url.Content("~/Content/mytemplate/")js/jquery.appear.js"></script>
    @*<script src="@Url.Content("~/Content/mytemplate/")js/cbpAnimatedHeader.js"></script>*@
    <script src="@Url.Content("~/Content/mytemplate/")js/owl.carousel.min.js"></script>
    <script src="@Url.Content("~/Content/mytemplate/")js/jquery.fitvids.js"></script>
    <script src="@Url.Content("~/Content/mytemplate/")js/styleswitcher.js"></script>

    <!-- Contact Form JavaScript -->
    <script src="@Url.Content("~/Content/mytemplate/")js/jqBootstrapValidation.js"></script>
    <script src="@Url.Content("~/Content/mytemplate/")js/contact_me.js"></script>

    <!-- Custom Theme JavaScript -->
    <script src="@Url.Content("~/Content/mytemplate/")js/script.js"></script>

